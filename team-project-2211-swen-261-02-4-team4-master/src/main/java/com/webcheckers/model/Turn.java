package com.webcheckers.model;

import java.util.ArrayList;
import java.util.List;

/**
 * array list of turns. useful for history and stacking moves when eating pieces.
 *
 * Handle checks too. It checks if it is still a players turn (if they have more moves to make
 *because of a double jump) for all checks not pertaining to who's turn it is, turn calls board which takes
 * care of all the checks based on the state of the board (where other pieces are)
 */
public class Turn {
//UNUSED


}
